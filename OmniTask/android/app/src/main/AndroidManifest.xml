<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ═══ NETWORK ═══ -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- ═══ FLOATING WIDGET (SYSTEM OVERLAY) ═══ -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- ═══ CALENDAR SYNC (Samsung Calendar) ═══ -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <!-- ═══ CONTACTS (Deep Link to Contact Cards) ═══ -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- ═══ SPEECH-TO-TEXT ═══ -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- ═══ MEDIA ATTACHMENTS (Photos, Videos, Voice, Screenshots) ═══ -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- ═══ VIBRATION (Task notifications) ═══ -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- ═══ DEEP LINK PACKAGE QUERIES (Android 11+ requirement) ═══ -->
    <queries>
        <!-- WhatsApp -->
        <package android:name="com.whatsapp" />
        <!-- Telegram -->
        <package android:name="org.telegram.messenger" />
        <!-- Samsung Notes -->
        <package android:name="com.samsung.android.app.notes" />
        <!-- Samsung Calendar -->
        <package android:name="com.samsung.android.calendar" />
        <!-- Intent schemes for deep linking -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="whatsapp" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="tg" />
        </intent>
    </queries>

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>

      <!-- ═══ FLOATING WIDGET FOREGROUND SERVICE ═══ -->
      <service
          android:name=".floatingwidget.FloatingWidgetService"
          android:enabled="true"
          android:exported="false"
          android:foregroundServiceType="specialUse" />

    </application>
</manifest>
